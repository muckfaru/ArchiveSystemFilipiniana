# Implementation Plan

- [ ] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - Category Values Not Saved or Retrieved
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Test concrete failing cases - files uploaded with categories that don't appear on public pages
  - Test implementation details from Fault Condition in design:
    - Verify Category field exists in form_fields table with field_label = 'Category'
    - Upload a test file with category selected (e.g., "Politics")
    - Query custom_metadata_values for the uploaded file's category value
    - Query public page category list: `SELECT DISTINCT cmv.field_value FROM custom_metadata_values cmv INNER JOIN form_fields cmf ON cmv.field_id = cmf.id WHERE cmf.field_label = 'Category'`
    - Assert category value is saved to custom_metadata_values with correct field_id
    - Assert public page query returns the category value
  - The test assertions should match the Expected Behavior Properties from design
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Category field missing from form_fields table?
    - Category field not in active form template?
    - Category values not saved to custom_metadata_values?
    - Field_id mismatch causing query to return no results?
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 2.1, 2.2, 2.3, 2.5_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Other Metadata Fields Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-category metadata fields:
    - Upload files with Title, Publisher, Publication Date, Language, Edition
    - Query custom_metadata_values for these fields
    - Verify values are saved correctly
    - Check public pages display these fields correctly
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - Test that Title field saves and displays correctly across many uploads
    - Test that Publisher field saves and displays correctly across many uploads
    - Test that Publication Date field saves and displays correctly across many uploads
    - Test that Language field saves and displays correctly across many uploads
    - Test that Edition field saves and displays correctly across many uploads
    - Test that edit functionality updates non-category metadata correctly
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [ ] 3. Fix for public page category display bug

  - [x] 3.1 Verify Category field exists in form_fields table
    - Query form_fields for field with field_label = 'Category'
    - If not found, identify root cause (migration issue, field not created, etc.)
    - Document findings for next step
    - _Bug_Condition: isBugCondition(input) where input.hasFile = true AND input.categorySelected = true AND (NOT categoryFieldExistsInFormFields() OR NOT categorySavedToCustomMetadataValues(input.fileId))_
    - _Expected_Behavior: Category field SHALL exist in form_fields table with correct field_type and field_options_
    - _Preservation: Other metadata fields in form_fields SHALL remain unchanged_
    - _Requirements: 2.5_

  - [x] 3.2 Ensure Category field is in active form template
    - Query form_templates for active form (is_active = 1)
    - Verify Category field is associated with active form template
    - If Category field missing, create migration or manual fix to add it
    - Populate field_options with categories from categories table
    - _Bug_Condition: Category field must be in active form to appear on upload form_
    - _Expected_Behavior: Category field SHALL be associated with active form template_
    - _Preservation: Other fields in active form template SHALL remain unchanged_
    - _Requirements: 2.5_

  - [x] 3.3 Create migration script to ensure Category field (if needed)
    - Create backend/migrations/006_ensure_category_field.php
    - Check if Category field exists in active form template
    - If missing, create Category field with correct field_type and field_options
    - Verify field_id is correctly assigned
    - Run migration and verify Category field is created
    - _Bug_Condition: Migration ensures Category field exists for all installations_
    - _Expected_Behavior: Migration SHALL create Category field if missing_
    - _Preservation: Migration SHALL NOT modify existing fields or data_
    - _Requirements: 2.5_

  - [x] 3.4 Verify upload logic saves category values correctly
    - Review admin_pages/upload.php lines 440-460 (metadata saving logic)
    - Verify field key construction: 'field_' . $field['id']
    - Verify select field values are sanitized and saved correctly
    - Confirm field_id matches Category field ID in form_fields
    - Test upload with category selected
    - Query custom_metadata_values to verify category value is saved
    - _Bug_Condition: Upload logic must save category values with correct field_id_
    - _Expected_Behavior: Category values SHALL be saved to custom_metadata_values with correct field_id_
    - _Preservation: Upload logic for other metadata fields SHALL remain unchanged_
    - _Requirements: 2.1_

  - [x] 3.5 Verify public pages query categories correctly
    - Test query: `SELECT DISTINCT cmv.field_value FROM custom_metadata_values cmv INNER JOIN form_fields cmf ON cmv.field_id = cmf.id WHERE cmf.field_label = 'Category'`
    - Verify query returns list of category names
    - Test browse.php displays correct categories for files
    - Test category filter in sidebar works correctly
    - _Bug_Condition: Public page queries must retrieve category values from custom_metadata_values_
    - _Expected_Behavior: Public pages SHALL display correct categories for all files_
    - _Preservation: Public page queries for other metadata fields SHALL remain unchanged_
    - _Requirements: 2.2, 2.3, 2.4_

  - [ ] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Category Values Saved and Displayed
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify all assertions pass:
      - Category field exists in form_fields table
      - Category values are saved to custom_metadata_values
      - Public page query returns category values
      - Files display correct categories on browse.php
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [ ] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - Other Metadata Fields Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix:
      - Title field saves and displays correctly
      - Publisher field saves and displays correctly
      - Publication Date field saves and displays correctly
      - Language field saves and displays correctly
      - Edition field saves and displays correctly
      - Edit functionality updates non-category metadata correctly
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run all exploration and preservation tests
  - Verify no regressions in upload functionality
  - Verify no regressions in public page display
  - Test full integration flow: upload with category → verify appears on public page with correct category
  - Test category filter flow: select category in sidebar → verify only files with that category are displayed
  - Ask the user if questions arise
